Place your Letterboxd diary export CSV here as:

`letterboxd-diary.csv`

Export it from Letterboxd settings (Import & Export). The site will
merge this archive with the live RSS feed so you get full history + new logs.
